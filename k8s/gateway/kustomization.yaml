# Gateway component kustomization.
# Gateway Kustomize overlay that ensures the gateway namespace label is applied before reconciling the shared Ingress resources.


apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace-label.yaml
  - ingress.yaml
