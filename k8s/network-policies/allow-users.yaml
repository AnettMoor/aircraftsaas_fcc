apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: allow-users-ingress, namespace: ns-users }
spec:
  podSelector: { matchLabels: { app: users-service } }
  policyTypes: [Ingress]
  ingress:
  - from:
    - namespaceSelector: { matchLabels: { name: ns-gateway } }
    - namespaceSelector: { matchLabels: { name: ns-fleet } }
    - namespaceSelector: { matchLabels: { name: ns-booking } }
    ports: [{ protocol: TCP, port: 8080 }]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: allow-users-egress, namespace: ns-users }
spec:
  podSelector: { matchLabels: { app: users-service } }
  policyTypes: [Egress]
  egress:
  # DNS (CoreDNS) — must use the auto-injected
  # kubernetes.io/metadata.name label, because kube-system does NOT have a
  # `name=kube-system` label by default on stock Kubernetes / kubeadm clusters.
  - to:
    - namespaceSelector:
        matchLabels: { kubernetes.io/metadata.name: kube-system }
      podSelector:
        matchLabels: { k8s-app: kube-dns }
    ports:
    - { protocol: UDP, port: 53 }
    - { protocol: TCP, port: 53 }
  - to:
    - namespaceSelector: { matchLabels: { name: ns-fleet } }
      podSelector:       { matchLabels: { app: fleet-service } }
    ports: [{ protocol: TCP, port: 8080 }]
  - to:
    - namespaceSelector: { matchLabels: { name: ns-booking } }
      podSelector:       { matchLabels: { app: booking-service } }
    ports: [{ protocol: TCP, port: 8080 }]
  # ns-infra egress is split per-pod (postgres + rabbitmq) rather than
  # one wildcard ns-infra entry. This matches the per-pod ingress side
  # in k8s/network-policies/allow-infra.yaml, so both caller and callee
  # are explicitly pinned. See that file's header for full rationale.
  - to:
    - namespaceSelector: { matchLabels: { name: ns-infra } }
      podSelector:       { matchLabels: { app: postgres } }
    ports: [{ protocol: TCP, port: 5432 }]
  - to:
    - namespaceSelector: { matchLabels: { name: ns-infra } }
      podSelector:       { matchLabels: { app: rabbitmq } }
    ports: [{ protocol: TCP, port: 5672 }]
